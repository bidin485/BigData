on:
  push:
    branches:
      - main  # Replace with your branch name if different

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    # Step 1: Run Tests
    - name: Run Unit Tests
      run: |
        npm install
        npm test  # Run your project's test script (adjust for your stack)

    # Step 2: Lint Code
    - name: Lint Code
      run: |
        npm install
        npm run lint  # Example for linting (adjust for your project)

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: Deploy via FTP
      uses: SamKirkland/FTP-Deploy-Action@3.1.1  # Use this supported version
      with:
        ftp-server: "ftp://46.17.175.181"  # Correct input key for FTP server
        ftp-username: "u510741299"  # Correct input key for FTP username
        ftp-password: "Bdal@2024"  # Correct input key for FTP password
        local-dir: ./  # Your local directory for deployment
        git-ftp-args: --insecure  # Optional arguments for FTP deployment (e.g., --insecure for SSL issues)
